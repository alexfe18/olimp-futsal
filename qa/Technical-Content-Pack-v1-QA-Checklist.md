# Sprint 04.2 — Technical Content Pack v1: QA Checklist

## Перед імпортом

- Відкрити `/admin/coach/exercises/import`.
- Для основного тесту використати JSON.
- Переконатися, що коди `TECH-001`–`TECH-015` ще не існують, або свідомо перевіряти duplicate flow.

## Preview — перший імпорт

Очікується:

- `Усього`: 15
- `Готові`: 15
- `Помилки`: 0
- `Дублікати`: 0
- усі рядки мають категорію `Техніка`;
- кнопка імпорту активна.

## Import

- Стратегія: `Пропустити дублікати`.
- Очікуваний результат: `Створено: 15`, `Оновлено: 0`, `Пропущено: 0`, `Помилки: 0`.

## Library

1. Фільтр категорії `Техніка` повертає 15 нових вправ.
2. Відображаються коди `TECH-001`–`TECH-015` без пропусків.
3. Назви чисті, без службових суфіксів.
4. Пошук знаходить щонайменше:
   - `підошва`;
   - `перший дотик`;
   - `1v1`;
   - `удар носком`.

## Details / Edit spot-check

Перевірити `TECH-001`, `TECH-007`, `TECH-012`, `TECH-014`, `TECH-015`:

- опис, організація та інвентар;
- тренерські акценти й типові помилки;
- progression, regression, alternative, load control;
- вікові групи, цілі та теги;
- складність, інтенсивність і розмір майданчика;
- джерело `Futsal Exercise Library — Technical Content Pack v1.0`;
- дата й час останнього оновлення;
- усі поля коректно завантажуються в Edit та зберігаються без втрат.

## Duplicate regression

- Повторно завантажити той самий JSON.
- Очікується: `Дублікати: 15`, `Готові: 0`, `Помилки: 0`.
- `Пропустити`: 15 пропущено, дані не змінені.
- `Оновити`: 15 оновлено, назви чисті, наявні медіа не видаляються.

## CSV parity

- Завантажити CSV після JSON-імпорту.
- Очікується 15 дублікатів без parser/validator errors.
- `age_groups`, `goals` і `tags` розділяються символом `|`.
